# Free key from https://dev.pokemontcg.io — never commit the real value.
# Used only by the /api/cards proxy (Vite dev server, Vercel, or Netlify).
POKEMONTCG_API_KEY=

# Example sold-range fixtures (Charizard mock, etc.). Dev already shows them.
# Prod defaults to hide the bar / "Solds unavailable" until a keyed TCGPlayer
# NM-raw feed exists. Set true only for staging review of the mock bar.
# VITE_SHOW_EXAMPLE_SOLDS=true
